import React from 'react';

const Navbar = (props) => {
  let {  } = props;

  return (
    <div>
      <span>HOME</span>
      <span>WORK</span>
      <span>ABOUT</span>
      <span>CONTACT</span>
    </div>
  )
}

export default Navbar;
